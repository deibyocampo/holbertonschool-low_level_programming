#include <stdio.h>
#include "holberton.h"
/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 **/
int main (void)
{
  int letras;
  for (letras = 'a'; letras <= 'z'; letras++)
    {
      _putchar(letras);
    }
  _putchar(letras);
return (0);
}
